## Drip Hackathon Context\n\n**Project:** Build Drip, an agent-native SaaS billing layer, for the Paygentic Week 2 BuildWithLocus hackathon.\n\n**Goal:** Win the \$1,000 prize by showcasing \"agentic AI\" within the given constraints of the competition.\n\n**Design Journey:**\n\n- Initial intent was to build something innovative, avoiding the obvious examples.\n- Shifted to building agent-native billing middleware (Drip) to demonstrate genuine agent autonomy, paired with a working demo of a personal async research agent (Deep Research Digest).\n- The plan evolved through several iterations, incorporating feedback from external AI agents to: reframe from a payments tool to an agent that runs your budget, make the agent loop more proactive, incorporate key demoable elements, answer key architectural questions, and add disaster recovery features.\n- Key is how we can present agent driven autonomous systems for billing & credits at each stage.\n\n**Key Architectural Decisions (Locked):**\n\n- **Credit model:** \"Logical ownership, physical pooling\" - One master PayWithLocus wallet owned by Drip; per-user USDC balance tracked in SQLite. User pays via Locus Checkout; Drip tracks and manages credits in SQLite and funds wrapped api calls for each call.\n- **Teardown:** Scale-to-zero using `PATCH /v1/services/{id}` with set `minInstances: 0` so service definition is preserved; for restore, we PATCH back to one; auto recovers quickly from crashes + funds never dissapear; but 2 API calls needed\n- **State persistence:** Two states, Drip uses SQLite + all shared research state in BWL managed Postgres\n- **Restore trigger:** Webhook (notifications about the state of the transfers and check points), but fallback by doing poll request every 60 seconds on the master wallet.\n- **We also ensure fast boot:** Prebuilt Docker image for container on GH repository with (linux/arm64).\n\n**Current stage:**\nCompleted a full engineering plan with the all Key decisions (file structure,\nSchema, and the implementation).The most important issue to begin with,\nto action is the creation and testing a BWL token. After that get one key step working where one container must spins up and restores (this verifies that the API functions correctly. To confirm, reach https://api.buildwithlocus.com/v1/ — this validates BWL).To begin to set and understand that Drip is running, check and understand a call to /health end point\n\n**Key Resources:**\n\n- Architecture approved for code design and local testing complete\n\n**Action Items:**\n\n- Kick off the first step and set the file system for Drip with the instructions in the engineering document, creating all necessary documents for the server architecture.\n\n- Follow engineering document after as primary guidance, but note all previous notes.\n\n## What's Left to Do\n- Build the components and wire it up.\n- Begin testing before pushing to prod.